# Merged over apphosting.yaml for the backend whose Environment name is
# "prod". See https://firebase.google.com/docs/app-hosting/multiple-environments
env:
  - variable: APP_ENV
    value: prod
    availability:
      - BUILD
      - RUNTIME
