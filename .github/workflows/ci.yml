name: Frontend E2E

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"
  pull_request:
    paths:
      - "frontend/**"

jobs:
  test-and-percy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"



      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Install Frontend Dependencies
        working-directory: frontend
        run: pnpm install

      - name: Install Functions Dependencies
        working-directory: functions
        run: pnpm install

      - name: Install Java (for Emulators)
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "21"

      - name: Cache Firebase emulators
        uses: actions/cache@v4
        with:
          path: ~/.cache/firebase/emulators
          key: ${{ runner.os }}-firebase-emulators-${{ hashFiles('firebase.json') }}
          restore-keys: |
            ${{ runner.os }}-firebase-emulators-

      - name: Start Emulators & Run Tests
        working-directory: frontend
        env:
          PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
          CYPRESS_CI: true
        run: |
          # Run visual tests only on:
          # 1. Push to main
          # Regular tests run on every synchronize (push to PR)

          if [[ "${{ github.event_name }}" == "push" && "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "Running with Percy..."
            pnpm exec percy exec -- pnpm run test:e2e:build
          else
            echo "Running regular tests (skipping Percy)..."
            pnpm run test:e2e:build
          fi
