.env

*.csv
*.json
*.jsonl
!package.json
!package-lock.json
!src/**/verified_selectors.json
!src/**/article_url_regexes.json

.coverage

downloaded*
versioned*
models

!src/**/*.json
!**/service_account.json
!**/config.json
!**/firebase.json
!src/**/patch.json
!src/**/*.identity.json
!src/stores/*.json
!src/entities/*.json

# Jupyter's autosaves next to the tracked notebooks under src/analysis
.ipynb_checkpoints

# from pytest-fast-first
.pytest-runtimes

# BIP crawler frontier + harvested bundles (iteration 1)
bip_crawl_out/

# The nightly's expected row counts and column coverage - tracked on purpose
!src/tests/e2e/baseline.json
